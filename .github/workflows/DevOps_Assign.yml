name: Greetings

on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Print Greetings
      run: echo "Learning Docker is made easy with this implementation"
